# Core AI framework
agno>=1.2.0
# Ollama local LLM
ollama>=0.4.0
# Gemini API (Google) — required when LLM_PROVIDER=gemini
google-genai>=1.0.0
# OpenAI API (GPT-4, GPT-3.5, etc.) — required when LLM_PROVIDER=openai
openai>=1.0.0
# Anthropic API (Claude) — required when LLM_PROVIDER=anthropic
anthropic>=0.25.0
# Vector store for RAG
chromadb>=0.5.0
# Gradio Web UI
gradio>=5.0.0
# Telegram bot
python-telegram-bot>=21.0
# Environment variables
python-dotenv>=1.0.0
# HTTP requests
httpx>=0.27.0
# Web search tool for agents
duckduckgo-search>=6.0.0
# Browser automation (MyTanita CSV sync + Garmin browser auth)
playwright>=1.40.0
requests-oauthlib>=1.3.0
# Garmin Connect integration
garminconnect>=0.2.22
# Encryption for per-user credential store
cryptography>=42.0.0
# Testing
pytest>=8.0.0